 <% if map.bookmarked_by?(current_user) %>
  <%= link_to map.bookmarks.count, map_bookmarks_path(map), method: :delete, remote: true, class: "fas fa-star star_yellow" %>
  <%else%>
  <%= link_to map.bookmarks.count, map_bookmarks_path(map), method: :post, remote: true, class: "fas fa-star  star_blue" %>
  <%end%>